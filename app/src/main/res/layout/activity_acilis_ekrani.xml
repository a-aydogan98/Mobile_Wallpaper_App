<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:background="@color/colorPrimary"
    tools:context=".Activity.AcilisEkraniActivity">

    <ImageView
        android:id="@+id/baslik_imageView"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/baslik_resim"
        android:layout_centerHorizontal="true"/>

    <TextView
        android:id="@+id/baslik_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:textSize="30sp"
        android:layout_below="@id/baslik_imageView"
        android:layout_marginTop="10dp"/>

    <ProgressBar
        android:id="@+id/baslik_progressBar"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/baslik_textView"
        android:layout_marginTop="50dp"/>

</RelativeLayout>